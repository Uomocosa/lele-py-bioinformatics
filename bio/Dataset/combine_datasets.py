from bio import *

# def combine_datasets(csv_files_input: List[Path], output_path: Path) -> Dataset:
#     datasets = [load(csv_file, max_dataset_size=config.max_size) for csv_file in csv_files_input]
#     return torch.utils.data.ConcatDataset(datasets)
